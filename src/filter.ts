import { 
  Branch,
  dataStore,
  ColumnIndex
} from './interfaces'

export function FilterData(data: dataStore, branch: Branch, owePeriod: number, balMin: number, balMax: number, exclude: string[]) {
  data

}